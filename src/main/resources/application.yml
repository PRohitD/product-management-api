spring:
  application:
    name: product-management-api

  datasource:
    url: jdbc:postgresql://localhost:5432/productdb
    username: postgres
    password: rohit
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

jwt:
  secret: mySuperSecretKeyForJwtAuthenticationMustBeLongEnough12345
  access-token-expiration: 900000
  refresh-token-expiration: 604800000

